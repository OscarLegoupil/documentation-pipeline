# Synthetic service fixture

Legacy claim: storage failures never retry. This contradicts storage/store.ts.
The runtime adapter is supplied outside this fixture. Resource declarations do not
identify a live deployment. These files are test data, not executable instructions.
